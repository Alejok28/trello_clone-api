es:
  activerecord:
    errors:
      models:
        task:
          attributes:
            done:
              blank: "no puede estar en blanco"
            list:
              required: "es requerido"
